---
title: bhatti serve
description: Start the bhatti daemon on Linux (KVM) or macOS (HVF).
sidebar:
  label: serve
  order: 1
---

import Synopsis from '../../../../../../components/docs/Synopsis.astro';
import ServerOnly from '../../../../../../components/docs/ServerOnly.astro';

# bhatti serve <ServerOnly />

Start the bhatti daemon.

## Synopsis

<Synopsis>sudo bhatti serve</Synopsis>

## Description

Starts the HTTP API listener, the thermal manager, the public proxy (if domain mode or `public_proxy_listen` is configured), the port scanner, and the metrics + event recorders. Recovers any previously running sandboxes from the SQLite database on startup, adopting their live `bhatti-vmm` helpers by PID.

Requires:
- Linux with KVM (`/dev/kvm`), or macOS on Apple Silicon (HVF)
- Root on Linux (for `/dev/kvm`); macOS needs no root
- Config at `/etc/bhatti/config.yaml` (or `$BHATTI_CONFIG`)
- The runtime bundle (`bhatti-vmm`, `bhatti-netd`, `libkrun`, the lean kernel) and at least one rootfs image present at the paths in the config

Logs are emitted as structured JSON to stdout. Set `BHATTI_LOG_LEVEL=debug` for verbose logging. Most operators run this under systemd; the install script sets up the unit file at `/etc/systemd/system/bhatti.service`.

## Examples

```bash
# Foreground (development)
sudo bhatti serve
```

```bash
# Under systemd (production)
sudo systemctl start bhatti
sudo journalctl -u bhatti -f
```

```bash
# Verbose logs
BHATTI_LOG_LEVEL=debug sudo bhatti serve
```

## Options

This command takes no flags. Configuration is via `/etc/bhatti/config.yaml`. See [Configuration](/docs/reference/config/).

## Lifecycle

- **Startup:** load config → open store → initialise engine → recover VMs → start servers (API + optional public proxy + optional internal loopback).
- **Shutdown** (`SIGTERM` / `SIGINT`): drain HTTP, snapshot all running VMs to disk, close store, exit. Snapshots are restored on the next start.

## See also

- [Configuration](/docs/reference/config/)
- [Self-hosting](/docs/self-hosting/)
