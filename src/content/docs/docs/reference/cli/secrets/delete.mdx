---
title: bhatti secret delete
description: Delete a secret. No confirmation prompt — be careful.
sidebar:
  label: secret delete
  order: 3
---

import Synopsis from '../../../../../../components/docs/Synopsis.astro';

Delete a secret.

## Synopsis

<Synopsis>bhatti secret delete &lt;name&gt;</Synopsis>

## Description

Removes the secret from the encrypted store. **No confirmation prompt** — unlike every other destructive command in the CLI, `secret delete` deletes immediately on invocation.

(This is an inconsistency the team has tracked for a fix. Until then, double-check the name.)

Sandboxes that already have the secret resolved into their environment keep running with the value they got at boot. Future creates that reference the deleted secret will fail with `secret "<name>" not found`.

## Examples

```bash
bhatti secret delete OLD_KEY
```

```text
deleted
```

## Options

This command takes only global flags. See [Global flags](/docs/reference/cli/#global-flags) for `--url`, `--token`, `--json`, `--timing`, `--data-dir`.

## See also

- [`bhatti secret list`](/docs/reference/cli/secrets/list/) — confirm the right name first
- [API: `DELETE /secrets/:name`](/docs/reference/api/#delete-a-secret)
